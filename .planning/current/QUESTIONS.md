# Questions

## Blocking
- Which framework should V0.1 use: Go, Rust, Python, or Node.js?

## Later
- Should the binary stay named `open-spinner`, or should the repo be `open-spinner` and the command be `agent-status`?
- Should V0.1 support WezTerm recipes, tmux recipes, or both first?
- Should first real adapter target Claude Code hooks or OpenCode plugins?
